# 프로젝트 스펙 문서 — QT 나눔

## 1. 프로젝트 개요

- **한 줄 설명**: 교회 QT(묵상) 소모임이 카카오톡 단톡방 대신 사용하는, QT 나눔 전용 공유 웹앱
- **타겟 사용자**
  - 개발자 본인이 속한 교회 QT 소모임 구성원 (소규모, 서로 아는 신뢰 기반 그룹)
- **해결하는 문제**: 카톡 단톡방에 QT 나눔(오늘 본문 느낀 점·삶의 적용)과 공지사항·잡담이 뒤섞여 나눔 글을 찾아보기 불편함
- **차별점**: 공지 없이 QT 나눔만 모이는 전용 공간. 관리자가 날짜별 주제·구절 참조만 짧게 입력해두면(본문 복사 없음), 성경 원문은 자동으로 붙여 "오늘의 QT" 화면으로 보여줌 — 큐티인 등 QT 교재 본문 자체는 저작권상 제공하지 않음
- **기간**: 2주 MVP
- **2026-08-12 확장**: 카톡 단톡방처럼 **그룹(방) 단위로 나눔이 분리**됨. 한 사람이 여러 그룹에 동시에 속할 수 있고, 초대 코드로 자유롭게 그룹을 만들거나 참여함(§4-8~4-10). 나눔·댓글·아멘만 그룹별로 분리되고, "오늘의 QT"·"말씀 등록"은 그룹과 무관하게 전체가 공유(§4-7)
- **2026-08-12 결정**: **댓글 기능은 일시 비활성화**(사용자 요청). DB 스키마·RLS·컴포넌트는 그대로 두고 화면에서만 주석 처리(§4-4 참고) — 필요해지면 주석만 해제해서 복구
- **2026-08-14 결정**: **PWA로 홈 화면 추가 지원**(§6 참고). 앱스토어/플레이스토어 등록은 하지 않고, 사파리·크롬에서 "홈 화면에 추가"만으로 아이콘+풀스크린 실행이 되게 함. Vercel 배포 전, 로컬 개발 서버를 ngrok으로 터널링해 실제 폰에서 먼저 확인 후 배포 진행 예정

---

## 2. 기술 스택 & 환경

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | **Next.js (App Router) + TypeScript** | 프론트+백엔드(API Route)를 한 프레임워크로 처리 → Vercel 무료 배포 최적, 별도 백엔드 서버 불필요 |
| Styling | **Tailwind CSS** | 빠른 개발, 카드 UI·반응형에 적합 |
| DB + Auth | **Supabase (Postgres)** | 무료 티어로 DB·인증 동시 해결, 카카오 OAuth 공식 지원 확인됨 |
| 로그인 | **카카오 로그인 (Supabase Auth의 Kakao Provider)** | 타겟 사용자 전원이 카카오 사용자, 별도 회원가입 마찰 없음 |
| 성경 본문 데이터 | **개역한글 오픈 데이터셋을 Supabase에 자체 시딩** | 개역개정은 무료 공개 API가 없음(저작권). 개역한글은 오픈소스 프로젝트에서 널리 쓰이는 데이터셋 존재 → 직접 DB에 임포트해 자체 조회 |
| 배포 | **Vercel(앱) + Supabase Cloud(DB/Auth), 둘 다 무료 티어** | 비용 없이 실제 모임원들이 접속 가능한 서비스로 배포 |
| 개발 환경 | macOS, Node 18+ | - |

---

## 3. 사용자 흐름

1. 접속 → 카카오 로그인
2. 로그인 후 **피드 탭 = 내 그룹 목록** — 카톡 채팅방 목록처럼, 내가 속한 QT 그룹들이 쭉 나열됨. 그룹이 하나도 없으면 그룹 만들기(이름만 입력)/초대 코드 참여 UI가 함께 보임
3. 네비게이션의 **"나눔"**(탭 라벨, 2026-08-12부로 "나눔 쓰기"에서 축약) → 작성 화면(그룹에 속박되지 않는 독립 작성, §4-2)
   - 캘린더에서 날짜 선택(기본값: 오늘) — 그 날짜에 이미 쓴 글이 있으면 폼 대신 읽기 전용 카드("나눔" 영역 + 수정하기·공유하기 버튼)를 바로 보여줌
   - 없으면 폼이 뜸: 고른 날짜에 등록된 QT 일정이 있으면 폼 맨 위에 "오늘의 QT"와 같은 스타일로 그날의 주제(굵게) + 전체 구절(amber)이 먼저 보임(2026-08-13 추가) → 그 아래 구절 참조 입력란에 참조가 자동으로 채워짐(수정 가능), 일정이 없으면 직접 입력 → 구절 원문 자동 표시(확인용) → 느낀 점/삶의 적용 텍스트 작성
   - 하단에 **두 개의 버튼**: **"저장하기"**는 그룹 선택 없이 바로 저장(개인 기록으로도 충분). **"공유하기"**는 팝업창을 열어 내가 속한 그룹을 체크박스로 하나 이상 골라(카톡 공유하기처럼 여러 그룹에 동시 공유 가능) 그 팝업 안에서 확정 — 그룹 화면에서 "나눔"으로 들어온 경우 그 그룹이 팝업에 기본 체크돼 있음
   - 저장/공유 완료 시 글이 기록되고(공유했다면 선택한 모든 그룹에도 동시에 뿌려짐), 화면은 그대로 나눔 작성 화면에 머물며 방금 쓴 내용이 읽기 전용 카드로 전환됨(상세 페이지로 이동하지 않음). 글에는 캘린더에서 고른 날짜가 그대로 기록됨(작성 시점의 오늘 날짜가 아님, 예: 어제 QT를 오늘 몰아 써도 날짜가 정확히 남음)
4. **그룹 클릭** → 그 그룹 전용 화면(고유 URL, `/groups/[id]`)으로 진입 — 상단에 캘린더가 처음부터 펼쳐져 있고(2026-08-13부로 "캘린더로 날짜 찾기" 토글 버튼 제거, 항상 노출), 그 아래에 이 그룹에 공유된 나눔만 **날짜별로 묶어서** 표시: 그날의 QT 주제를 헤더로 한 번만 보여주고(성경 원문은 목록에 없음, 다들 이미 아는 말씀이라는 전제), 그 아래에 그날 이 그룹에 공유된 나눔(닉네임 + 나눔 내용)이 나열됨. 캘린더에서 날짜를 고르면 그 날짜만 필터링(선택 후에도 캘린더는 계속 열려 있음, "← 전체 보기"로 필터 해제). 화면 상단에 뒤로가기 버튼(← 뒤로)으로 이전 화면(피드 목록)으로 돌아갈 수 있음
5. 그룹 화면에서 특정 나눔 클릭 → 상세 화면: 그 날짜의 QT 소주제 + 나눔 본문(구절 원문은 생략) + 아멘 버튼 (댓글은 2026-08-12부로 일시 비활성화, §4-4). 여기도 상단에 뒤로가기 버튼 있음
6. 아멘(반응) 누르기
7. 본인 글은 수정·삭제 가능(수정은 내용·구절만, 공유 그룹 변경은 지원 안 함)
8. 상단 네비게이션에서 **"오늘의 QT"**로 이동 — 캘린더 없이, 오늘 날짜의 주제 + 구절(소주제 구분 포함) + 본문요약 + 중보기도를 우리들교회달력 "묵상하기" 탭과 유사한 형태로 보여줌(그룹과 무관하게 전체 공유)
9. **(관리자 전용)** 네비게이션의 **"말씀 등록"** 메뉴로 이동 — 캘린더에서 날짜를 고르고 그 날짜의 주제·구절·소주제·본문요약·중보기도를 등록·수정(그룹과 무관, 전체 1개). 일반 모임원에게는 이 메뉴 자체가 안 보임

---

## 4. 기능별 입출력 스펙

### 4-1. 카카오 로그인
- **트리거**: 로그인 버튼 클릭
- **처리**: Supabase Auth `signInWithOAuth({ provider: 'kakao' })`
- **출력**: 세션 생성, 최초 로그인 시 `profiles` 테이블에 닉네임·아바타 저장
- **검증**: 로그인 후 새로고침해도 세션 유지, 로그아웃 시 피드 접근 불가

### 4-2. 나눔 글 작성 — 하루에 하나, 쓴 건 읽기 전용으로 표시 (2026-08-13 10차 수정, 최종)
- **입력**: 날짜(캘린더 선택, 기본값 오늘)만 먼저 고름
- **처리**: 날짜를 고르면 **그 날짜에 내가 이미 쓴 글이 있는지부터 확인**
  - **이미 쓴 글이 있으면**: 폼 대신 그 글을 그대로 **읽기 전용 카드**로 보여줌 — 그 날짜 `qt_schedule`의 주제(굵게)를 맨 위에 보여주고(2026-08-13 추가), 구절은 소주제가 등록돼 있으면 작성 폼과 동일하게 소주제별로 나눠서, 없으면 글 저장 당시 스냅샷(`post.verse_text`) 그대로 통짜로 표시(관리자가 나중에 주제·소주제 구성을 바꾸면 이 카드도 최신 구성을 따라감). 그 아래 "나눔" 라벨 + 박스 안에 나눔 내용을 표시. 카드 하단에 **"수정하기"**(상세 페이지 §4-5로 이동해 고침) / **"공유하기"**(팝업으로 그룹 체크박스, `add_shares_to_post` DB 함수로 이 글에 그룹을 추가로 공유) 두 버튼만 있음
  - **안 썼고 그 날짜에 등록된 QT 일정이 있으면**: 폼 맨 위에 "오늘의 QT"와 같은 스타일로 그날의 주제(굵게) + 전체 구절(amber) + 소주제별 구절(등록돼 있으면)을 고정으로 보여주고, 그 아래 나눔 텍스트만 쓰는 칸이 있음. **구절 참조를 직접 입력하는 기능은 2026-08-13부로 폐지** — 나눔은 항상 그날 QT 일정의 구절을 그대로 사용(`qt_schedule.verse_ref`/`verse_text`를 그대로 `posts`에 스냅샷). 하단에 **"저장하기"**(그룹 선택 없이 즉시 저장) / **"공유하기"**(팝업으로 그룹 체크박스, 내가 속한 그룹 중 다중 선택 후 확정) 두 버튼
  - **안 썼고 그 날짜에 등록된 QT 일정이 없으면**: 폼 대신 "아직 등록된 QT가 없어요. 관리자가 이 날짜의 QT를 등록하면 나눔을 쓸 수 있어요." 안내만 뜨고 작성 자체가 불가능
  - 저장/공유 성공 시 `create_post_with_shares` DB 함수(security definer)로 새 글 생성 + 그룹 공유(0개여도 됨)를 한 트랜잭션 처리 → 화면은 그대로 나눔 쓰기 화면에 머물고, 방금 쓴 내용이 바로 위 읽기 전용 카드로 전환됨
- **출력**: 글이 저장되고, 선택한 그룹이 있으면 그 그룹 화면들의 해당 날짜 묶음에도 즉시 반영. 글의 날짜는 고른 날짜 그대로 저장(작성 시점의 오늘 날짜가 아님, 예: 어제 날짜를 골라 오늘 몰아 써도 정확히 남음)
- **검증**: 이미 쓴 날짜를 다시 골라도 폼이 아니라 읽기 전용 카드가 뜸(중복 작성 방지). 그룹을 하나도 안 골라도 저장은 정상적으로 됨. 읽기 전용 카드에서 "공유하기"로 그룹을 추가해도 이미 공유된 그룹은 중복 없이(`on conflict do nothing`) 처리됨
- **검증**: 나눔 텍스트 빈 값이면 게시 불가. QT 일정이 없는 날짜는 안내만 뜨고 폼 자체가 없어서 작성 불가(구절 직접 입력 폐지 이전엔 가능했음 — 폐지 이유·과거 동작은 TODO.md 참고). 내가 속하지 않은 그룹에는 공유 자체가 서버에서 거부됨. 공유 팝업에서 그룹을 하나도 안 고르면 확정 버튼이 비활성화됨. 작성자 본인은 공유 여부와 상관없이 항상 자기 글을 볼 수 있음(RLS)

### 4-3. 그룹 화면 (날짜별 나눔 목록)
- **입력**: 그룹 ID(URL), 로그인 세션
- **처리**: 이 그룹에 공유된(`post_shares.group_id` 일치) 글을 전부 조회해 `qt_date` 기준으로 묶고, 날짜 내림차순 정렬. 묶음마다 그 날짜의 `qt_schedule.title`을 함께 조회(없으면 제목 생략하고 날짜만)
- **출력**: 상단에 캘린더가 처음부터 펼쳐져 있음(2026-08-13부로 토글 버튼 없이 항상 노출). 그 아래 날짜 헤더(주제 포함) 아래 그 날짜에 이 그룹에 공유된 나눔이 나열 — 작성자 닉네임, 나눔 본문 일부, 아멘 수만 표시(구절 원문은 목록에서 생략, 상세에서만 보임. 댓글 수는 2026-08-12부로 비활성화). 이 그룹의 멤버가 아니면 접근 불가(리다이렉트). 캘린더에서 날짜를 고르면 그 날짜만 필터링해서 볼 수 있음("← 전체 보기"로 해제 가능)
- **검증**: 새 글을 이 그룹에 공유하면 그 날짜 묶음에 즉시 반영(묶음이 없었으면 새로 생김). 다른 그룹에만 공유된 글은 전혀 안 보임. 같은 글이 여러 그룹에 동시에 공유돼 있으면 각 그룹 화면에 각각 나타남(아멘은 글 자체에 달리므로 모든 공유 그룹에서 공통으로 보임)

### 4-4. 나눔 상세 + 아멘 (댓글은 2026-08-12부로 일시 비활성화)
- **입력**: 글 ID
- **출력**: 그 날짜의 QT 소주제(있으면) + 나눔 본문(영역으로 구분) + 아멘 수. 구절 원문은 2026-08-12부로 상세 화면에서도 생략(그룹 목록과 동일하게 "다들 이미 아는 말씀" 전제) — DB에는 그대로 저장되고 수정 폼에서는 계속 보임
- **처리**: 아멘 토글(사용자당 글 하나에 1회, 다시 누르면 취소)
- **비활성화 메모**: 댓글 DB 테이블(`comments`)·RLS·컴포넌트(`CommentList`, `CommentForm`)는 삭제하지 않고 그대로 둠. 화면 쪽 렌더링/쿼리만 주석 처리(`app/(protected)/posts/[id]/page.tsx`, `app/(protected)/groups/[id]/page.tsx`, `components/PostCard.tsx`) — 필요해지면 주석만 해제해서 복구
- **검증**: 아멘 중복 클릭 시 취소로 동작(카운트 증가 안 함)

### 4-5. 글 수정·삭제
- **입력**: 글 ID, 작성자 본인 확인
- **출력**: 수정된 내용 반영 / 목록에서 제거
- **검증**: 본인 글이 아니면 수정·삭제 버튼 자체가 보이지 않음

### 4-6. 오늘의 QT (독립 페이지, 캘린더 없음)
- **트리거**: 네비게이션의 "오늘의 QT" 클릭
- **입력**: 없음 — 항상 오늘 날짜 고정
- **처리**: 오늘 날짜의 `qt_schedule` 행 조회(주제·구절 참조·소주제·본문요약·중보기도)
- **출력**:
  - 일정이 있으면: 날짜 + 주제 + 구절(소주제로 나뉘어 있으면 소주제별로, 없으면 전체 하나로) + 본문요약 + 중보기도
  - 일정이 없으면: "아직 등록된 QT가 없어요" 안내 문구
- **검증**: 캘린더·날짜 이동 없이 오늘 것만 보임(2026-08-12부터 단순화 — 다른 날짜 조회는 §4-2 나눔 쓰기, §4-7 말씀 등록에서 캘린더로 가능)

### 4-7. QT 일정 등록·수정 (관리자 전용, 독립 페이지)
- **트리거**: 네비게이션의 "말씀 등록" 클릭(관리자 계정에만 메뉴가 보임)
- **입력**: 캘린더에서 고른 날짜, 주제, 전체 성경 구절 참조, (선택) 소주제별 구절 목록(소주제명 + 절 번호, 2026-08-13부로 책/장은 전체 구절에서 자동으로 가져와서 절 번호만 입력하면 됨 — 예: 전체 구절이 "신명기 6:16-25"면 소주제엔 "16-19"처럼 절만 입력), (선택) 본문요약, (선택) 중보기도
- **처리**: 구절 참조마다 `bible_verses` 조회 → 원문 스냅샷과 함께 `qt_schedule`에 upsert(같은 날짜 재등록 시 덮어씀). 소주제별 구절은 `sections` 컬럼(JSON)에 저장. 캘린더에서 다른 날짜를 고르면 그 날짜의 기존 등록 내용이 폼에 자동으로 채워짐(수정 목적)
- **출력**: 해당 날짜의 "오늘의 QT"(오늘 날짜인 경우) 및 "나눔 쓰기" 자동완성에 즉시 반영
- **검증**: 관리자가 아닌 계정에는 이 메뉴·페이지 자체가 안 보이고 직접 URL 접근도 리다이렉트됨, 서버(RLS)에서도 관리자가 아니면 쓰기 자체가 거부됨
- **저작권 참고(2026-08-11 결정)**: 주제·소주제명·본문요약·중보기도는 QTin 등 QT 교재 내용을 관리자가 그대로 옮겨 적는 것을 **현재는 허용**한다(§6 원래 원칙의 예외, 사용자 명시적 승인). 성경 구절 원문만 자동 표시하던 기존 원칙보다 완화된 결정이며, 소규모 비공개 모임 전용 사용을 전제로 한다. 추후 자체 콘텐츠로 교체 검토(TODO.md 참고)

### 4-8. 그룹 목록 (피드 탭, 2026-08-12 추가)
- **트리거**: 로그인 직후, 또는 네비게이션의 "피드" 클릭
- **입력**: 없음 (로그인 세션)
- **처리**: 내가 속한 그룹 목록 조회
- **출력**: 카톡 채팅방 목록처럼 그룹 이름 나열, 각 그룹 클릭 시 `/groups/[id]`로 이동. 그룹이 하나도 없으면 목록 대신 안내 문구 + 그룹 만들기/참여 UI 강조
- **검증**: 다른 그룹에 속한 사람에게는 내 그룹이 안 보임(그룹 목록 자체가 멤버십 기반)

### 4-9. 그룹 만들기 (2026-08-12 추가, 버튼 UX는 같은 날 2차 수정)
- **트리거**: "그룹" 화면의 **"+ 그룹 만들기"** 버튼(처음엔 폼이 안 보임, 버튼을 눌러야 이름 입력 폼이 펼쳐짐 — "취소"로 다시 접힘)
- **입력**: 그룹 이름
- **처리**: `create_group(name)` DB 함수(security definer) 호출 — 그룹 생성 + 만든 사람을 그룹 멤버로 자동 등록 + 초대 코드 자동 발급. 클라이언트가 `groups`/`group_members` 테이블에 직접 insert하지 않음(RLS로 막혀 있음, 함수로만 가능)
- **출력**: 새 그룹이 내 그룹 목록에 추가되고, 그 그룹 화면(`/groups/[id]`)으로 바로 이동. 피드 탭의 그룹 목록 카드마다 초대 코드 옆에 "복사" 버튼이 있어 눌러서 바로 클립보드에 복사 가능(2026-08-14 추가, `CopyInviteCode` 컴포넌트) — 버튼을 눌러도 그룹 화면으로 이동하지 않음
- **검증**: 누구나(관리자 아니어도) 그룹을 만들 수 있음. 그룹 개수 제한 없음

### 4-10. 초대 코드로 그룹 참여 (2026-08-12 추가, 버튼 UX는 같은 날 2차 수정)
- **트리거**: "그룹" 화면의 **"+ 초대 코드로 참여"** 버튼(처음엔 폼이 안 보임, 버튼을 눌러야 코드 입력 폼이 펼쳐짐 — "취소"로 다시 접힘)
- **입력**: 초대 코드 문자열
- **처리**: `join_group_by_code(code)` DB 함수 호출 — 코드에 해당하는 그룹을 찾아 나를 멤버로 등록. 이미 멤버면 조용히 무시(에러 아님)
- **출력**: 해당 그룹이 내 그룹 목록에 추가되고, 그 그룹 화면으로 바로 이동
- **검증**: 존재하지 않는 코드 입력 시 "유효하지 않은 초대 코드예요" 에러 문구. 다른 그룹의 나눔·글 목록을 코드 없이는 조회·추측할 수 없음(그룹 목록 자체가 비멤버에게 노출 안 됨)

---

## 5. 의사결정 규칙

AI가 애매할 때 멈추거나 묻지 말고 *이 순서대로* 따를 기준.

### 기술 선택
- *SPEC.md에 명시된 스택 고정.* 다른 프레임워크로 갈아타지 말 것.
- *새 의존성 추가 전 반드시 질문.* 추가 확정 시 이유 한 줄과 함께 CLAUDE.md에 기록.
- *의존성 고민 시*: 다운로드 수 가장 많고 Next.js 공식 예제에 있는 것 우선.

### 코드 스타일 (공통)
- *주석은 WHY만.* WHAT은 이름으로.
- *함수 30줄 넘으면 쪼개기.*
- *에러 핸들링은 시스템 경계만* (Supabase 호출, 카카오 로그인, API Route). 내부 함수는 fail fast.

### 코드 스타일 (TypeScript)
- *`any` 금지.* 불가피할 땐 `unknown` + 타입 좁히기.
- *상태 관리는 useState / useReducer만.* Redux / Zustand 금지.
- *서버 상태(피드·댓글 등)는 Server Component 또는 단순 fetch로.* 별도 데이터 페칭 라이브러리(React Query 등) 금지 — MVP 규모에 과함.
- *컴포넌트 파일 200줄 넘으면 쪼개기.*
- *Tailwind 유틸리티 우선.* 커스텀 CSS 최소화.

### 데이터 처리 (성경 구절)
- *구절 참조 파싱 실패 시 에러 문구만 표시, 앱 죽이지 말 것.*
- *글에는 항상 원문 스냅샷을 저장.* 조회 시점에 매번 `bible_verses` 재조회하지 않음(데이터 변경에도 과거 글 보존).

### 인증·권한
- *로그인 안 하면 어떤 화면도 못 봄.* 공개 열람 없음(하지 말 것 §6 참조).
- *본인 글/댓글만 수정·삭제 가능.* 서버(RLS)에서 `user_id` 비교로 강제, 프론트 숨김만으로 끝내지 말 것.
- *나눔·댓글·아멘에는 리더/관리자 권한 같은 역할 구분 없음.* 모든 모임원 동일 권한.
- **예외**: *QT 일정(`qt_schedule`) 등록·수정만 관리자(`profiles.is_admin = true`) 전용.* 관리자 지정은 앱 UI로 만들지 않고, 최초 1회 Supabase 대시보드에서 사용자가 직접 본인 행에 플래그를 켬(CLAUDE.md 참조). 이 예외 하나만 인정하고 그 이상 역할 시스템으로 확장하지 말 것.

### 디자인
- *디자인 결정은 AI가 자율적으로 처리.* 사용자에게 색/레이아웃/폰트 선택을 묻지 말 것.
- *기본 팔레트*: Tailwind `stone`(따뜻한 그레이) + `amber`(포인트, 말씀/묵상 느낌). 색은 최대 3가지.
- *폰트*: 시스템 기본. 웹폰트 추가 금지.
- *레이아웃*: 흰/아이보리 배경 + 카드 UI. 반응형 필수(`sm:` 이상), 모바일 우선(모임원 대부분 폰으로 접속 예상).
- *사용자 피드백 받은 후에만 수정.* 스스로 재작업 금지.

### 스코프
- *SPEC.md "하지 말 것" 목록 절대 건드리지 말 것.*
- *복잡해지면 하드코딩으로 먼저 돌아가게.* 추상화는 두 번째 구현 때.

### 그룹 (2026-08-12 추가)
- *그룹 생성·참여는 `create_group`/`join_group_by_code` DB 함수로만.* 클라이언트가 `groups`/`group_members`에 직접 insert하지 않음(초대 코드 없이 그룹을 찾거나 무단 가입하는 걸 막기 위함).
- *그룹 생성에 관리자 권한 필요 없음.* 아무나 그룹을 만들 수 있음 — `is_admin`은 QT 일정 등록 전용 개념과 별개.
- *그룹 삭제·멤버 추방·그룹장 권한 위임 같은 관리 기능은 만들지 말 것.* 지금은 "목록 보기/만들기/참여하기"까지만(YAGNI).
- *"어느 그룹을 보고 있는지"는 URL로.* `/groups/[id]` 라우트 파라미터가 곧 현재 그룹 — 쿠키·전역 상태 불필요, 링크 공유·북마크도 자연스럽게 됨.

### 파일 / 폴더
- *`app/`, `components/`, `lib/`, `types/` 4개 최상위 폴더만.* 불필요한 서브폴더 금지.
- *파일 삭제 전 반드시 질문.*

### 판단 자체가 안 설 때
- *가장 단순한 쪽.* MVP의 적은 복잡도가 최우선.
- *그래도 모르면 질문.* 단, 질문은 명확한 선택지 2~3개로 제시할 것.

---

## 6. 하지 말 것

- ❌ 카카오 로그인 외 다른 로그인 방식(이메일/비번, 구글 등)
- ❌ 그룹 삭제, 멤버 추방, 그룹장/부그룹장 같은 그룹 내 역할 구분 (§4-8~4-10 "만들기/참여/전환"까지만 허용, 2026-08-12부로 다중 그룹 자체는 허용됨 — 이전 버전의 "다중 그룹 지원 없음" 조항 폐기)
- ❌ 나눔·댓글·아멘에 대한 리더 권한(승인·강제삭제 등), 멤버 초대·승인 관리 — **QT 일정 등록 권한만 §4-7·§5의 단일 예외**
- ❌ 관리자 지정을 위한 앱 내 UI(멤버 관리 화면 등) — Supabase 대시보드에서 수동으로만
- ❌ 공지사항 게시판 (여전히 카톡에서 처리, 이 앱은 나눔 전용)
- ⚠️ QT 교재(큐티인 등) 주제·본문요약·중보기도는 관리자가 수동으로 옮겨 적는 것을 §4-7 결정에 따라 허용(소규모 비공개 모임 전제, 자동 수집·크롤링은 여전히 금지)
- ❌ 아멘 외 다양한 이모지 반응
- ❌ 알림(푸시/이메일) 기능
- ❌ 나눔 글 검색·태그·카테고리 기능 (단, "오늘의 QT"·구절 원문 조회는 §4-6·§4-7에 명시된 별도 기능이며 여기 해당 안 됨)
- ⚠️ **PWA는 2026-08-14 결정으로 허용됨** — 사용자가 폰에서 "네이티브 앱처럼" 쓰고 싶어함(앱스토어 등록은 원하지 않음). `app/manifest.ts`(Web App Manifest) + `app/layout.tsx`의 `appleWebApp`/`viewport.themeColor`/`other['apple-mobile-web-app-capable']` 메타데이터로 홈 화면 추가 시 풀스크린 standalone 실행 지원. **오프라인 캐싱(서비스 워커)은 여전히 범위 밖** — 순수 "홈 화면 아이콘 + 풀스크린 실행"까지만, 네트워크 끊겼을 때 동작하는 오프라인 지원은 하지 않음(이 앱은 어차피 로그인·실시간 데이터가 핵심이라 오프라인 의미가 적음)
- ❌ i18n(다국어)

---

## 7. 검증 기준

### MVP 성공 조건
- [ ] 카카오 로그인 후 피드 탭(내 그룹 목록) 진입 성공
- [ ] 그룹이 없는 신규 계정은 그룹 만들기·초대 코드 참여 UI가 보이고, 둘 다 정상 동작함
- [ ] 그룹 클릭 시 그 그룹의 고유 URL(`/groups/[id]`)로 이동, 날짜별로 묶여서 그날의 QT 주제 + 그룹원들의 나눔이 표시됨(말씀 원문은 목록에 안 보임)
- [ ] 관리자 계정으로 "말씀 등록" 페이지에서 QT 일정(주제+구절)을 등록하면 즉시 반영됨
- [ ] 일반 모임원 계정에는 "말씀 등록" 메뉴 자체가 보이지 않고, 직접 URL로 들어가도 막힘
- [ ] "오늘의 QT" 화면에서 오늘 날짜의 주제+구절 참조+성경 원문이 캘린더 없이 표시됨
- [ ] "나눔" 캘린더에서 날짜를 선택하면 그 날짜의 QT 일정(주제+구절)이 오늘의 QT와 같은 모양으로 보이고, QT 일정이 없는 날짜는 안내만 뜨고 작성 불가
- [ ] 나눔 쓰기에서 그룹을 여러 개 체크해 공유하면, 체크한 모든 그룹 화면의 그 날짜 묶음에 동시에 반영됨. 그룹 화면에서 "나눔 쓰기"로 들어가면 그 그룹이 기본 체크돼 있음
- [ ] 그룹 화면 상단에 캘린더가 처음부터 펼쳐져 있고, 날짜를 고르면 그 날짜만 필터링해서 볼 수 있고, "전체 보기"로 해제됨
- [ ] 서로 다른 그룹에 속한 계정끼리는 상대 그룹에만 공유된 글을 전혀 못 봄(그룹 목록에도 안 뜨고, URL 직접 접근도 막힘)
- [ ] 같은 그룹에 속한 다른 모임원 계정으로 로그인하면 그 그룹에서 같은 나눔이 보임(실제 공유 확인)
- [ ] 아멘 토글이 정상 동작 (댓글은 2026-08-12부로 일시 비활성화, 검증 대상 아님)
- [ ] 본인 글만 수정·삭제 버튼이 보임
- [ ] 모바일 화면에서 레이아웃 깨지지 않음
- [ ] Vercel 배포 URL로 실제 접속 가능

### 검증 대상
- 개발자 본인
- 교회 QT 모임원 (실사용 테스트)
