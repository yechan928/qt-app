# TODO — QT 나눔

앞으로 고민하고 결정해야 할 사항들.

---

## 1. QT 교재(QTin) 내용 직접 옮겨 적기 — 저작권 리스크

### 현재
- 2026-08-11 결정으로 "오늘의 QT" 화면의 소제목·소주제명·본문요약·중보기도를 관리자가 QTin 책을 보고 그대로 옮겨 적는 방식으로 구현함(SPEC.md §4-7, §6 참고)
- 성경 원문(`bible_verses`)만 자동 생성이고, 나머지 텍스트는 전부 QTin 편집팀의 저작물을 수동 복제한 것
- 자동 수집·크롤링은 안 함, 소규모 비공개 모임(카톡 대체) 용도로만 사용한다는 전제

### 문제
- QTin은 두란노(대한예수교장로회 등)에서 발행하는 유료 출판물로, 소제목·해설·요약·기도문은 명확히 저작권이 있는 편집 콘텐츠
- 비공개·소규모·비영리라는 사정이 있어도 복제 자체는 저작권법상 완전히 안전하지 않음
- 관리자가 매일 직접 타이핑하는 구조라 자동화된 대량 복제는 아니지만, 누적되면 사실상 책 상당 부분이 재현될 수 있음

### 결정해야 할 것
- 이 방식을 계속 유지할지, 아니면 관리자가 자기 언어로 다시 풀어쓴 요약/기도문으로 바꿀지
- 앱을 실제 배포(Vercel 등 외부 접근 가능한 URL)한 뒤에도 계속 이렇게 할지 — 비공개 링크 공유 수준을 넘어서면 리스크가 커짐
- 나중에 모임 규모가 커지거나 앱을 다른 교회와 공유하게 될 경우 이 결정을 반드시 재검토

---

## 2. 댓글 기능 — 일시 비활성화 상태

### 현재
- 2026-08-12 사용자 요청으로 댓글 기능을 화면에서 껐음. DB 테이블(`comments`)·RLS·컴포넌트(`CommentList`, `CommentForm`)는 삭제하지 않고 그대로 둠 — 렌더링/쿼리 부분만 주석 처리(`app/(protected)/posts/[id]/page.tsx`, `app/(protected)/groups/[id]/page.tsx`, `components/PostCard.tsx`)
- 나눔 상세 화면에는 이제 아멘만 남음

### 결정해야 할 것
- 다시 켤지, 아니면 완전히 뺄지(그럴 경우 DB 마이그레이션으로 테이블도 정리)는 나중에 판단. 지금은 주석만 해제하면 바로 복구 가능한 상태로 유지

---

## 3. 나눔 작성이 관리자의 QT 등록에 의존하게 됨 (2026-08-13 결정)

### 현재
- 나눔 쓰기에서 "말씀 구절" 직접 입력 기능을 완전히 없앰(사용자 명시적 결정 — 입력란+미리보기 버튼이 위에 자동으로 뜨는 그날의 주제/구절과 중복돼 보인다는 이유). 이제 나눔은 항상 그날 `qt_schedule`에 등록된 구절만 사용
- 그 결과: **관리자가 그날의 QT를 "말씀 등록"에 미리 넣어두지 않으면, 그 날짜엔 아무도 나눔을 못 씀**("아직 등록된 QT가 없어요" 안내만 뜨고 폼 자체가 없음). 이전엔 QT 미등록 날짜도 구절을 직접 입력해서 나눔 작성이 가능했음(SPEC.md §4-2 9차 이전)

### 결정해야 할 것
- 관리자가 깜빡하고 QT를 늦게 등록하는 날이 실제로 생기면(예: 하루 지나서 등록), 그 사이 모임원들이 나눔을 못 쓰는 문제가 실사용에서 얼마나 불편한지 지켜볼 것
- 불편하면: (a) 예전처럼 구절 직접 입력을 다시 허용하거나, (b) 관리자용 리마인더를 추가하는 방향을 재검토(단, (b)는 SPEC §6 "알림 기능 없음"과 충돌하므로 별도 논의 필요)

---

## 4. 로그아웃해도 카카오 자체 세션은 안 끊김 — 같은 계정으로만 재로그인됨

### 현재
- 앱의 "로그아웃"은 우리 앱(Supabase) 세션만 지움. 카카오 자체 로그인 세션은 그대로라서, 로그아웃 후 "카카오로 로그인"을 다시 눌러도 계정 선택 화면 없이 방금 전 썼던 그 카카오 계정으로 바로 재로그인됨
- 지금은 다른 계정으로 테스트하려면 카카오 자체를 로그아웃하거나, 시크릿 모드/다른 브라우저를 써야 함(2026-08-14 확인)

### 결정해야 할 것
- 실사용(여러 모임원이 각자 폰에서 자기 카카오 계정으로만 씀)에서는 사실 문제되지 않을 가능성이 높음 — 계정 전환은 개발 중 여러 테스트 계정을 써볼 때만 걸리적거리는 문제라 실사용 확인 후 그래도 불편하면 재검토
- 고칠 경우 후보: 카카오 authorize 요청에 재인증을 강제하는 파라미터를 추가하는 방향(카카오 OAuth 문서에서 지원 여부 확인 필요)

---

## 완료된 항목

- ✅ 2026-08-11 — 카카오 로그인 연동 (KOE004/KOE205 트러블슈팅: 동의항목 활성화, 비즈 앱 전환, account_email 선택 동의 필요했음)
- ✅ 2026-08-11 — 개역한글 성경 원문 3만여 구절 시딩, 원본 데이터의 불필요한 `!`/`?` 정리
- ✅ 2026-08-11 — 오늘의 QT에 절 번호 표시, 소주제별 구절 구분, 본문요약·중보기도 필드 추가
- ✅ 2026-08-12 — 그룹(방) 단위 나눔 분리 구현: `groups`/`group_members`/`post_shares` 테이블, 초대 코드로 참여, 카톡 공유하기처럼 한 나눔을 여러 그룹에 동시 공유. 여러 차례 방향 수정 끝에 "하루에 하나, 이미 쓴 건 읽기 전용" 모델로 정착(SPEC.md §4-2·§4-8~4-10, PLAN.md §K)
- ✅ 2026-08-12 — 이미 쓴 글 미리보기(`NanumPreview`)에 "공유하기" 버튼 추가, `add_shares_to_post` RPC 연결 (기존엔 만들어만 두고 UI에서 미사용이었음)
- ✅ 2026-08-13 — 그룹 화면 캘린더를 토글 버튼 없이 항상 펼침, 그룹 만들기/참여를 "+" 버튼 뒤로 숨김(CreateGroupSection/JoinGroupSection), 나눔 상세·그룹 화면에 뒤로가기 버튼 추가, 말씀 등록 폼의 "소제목"을 "주제"로 이름 변경, 소주제별 구절을 절 번호만 입력하면 되도록 단순화(책/장은 전체 구절에서 자동 상속), 나눔 쓰기에서 구절 직접 입력을 없애고 그날 QT 일정 구절만 쓰도록 변경(§3 참고)
- ✅ 2026-08-14 — 휴대폰에서 실제로 화면 확인하려고 로컬 개발 서버를 ngrok으로 터널링(공유기 client isolation 때문에 같은 와이파이 LAN IP 직접 접속이 안 됐음). 그 과정에서 Next.js dev 서버가 ngrok 도메인의 정적 파일/HMR 요청을 보안상 차단하던 버그 발견 → `next.config.ts`에 `allowedDevOrigins` 추가로 해결. PWA(홈 화면 추가) 지원 추가 — SPEC §6의 "PWA 없음" 결정을 뒤집음(§1 참고), `app/manifest.ts` + `app/layout.tsx` 메타데이터 + `public/icons/` 아이콘 3종 추가. 오프라인 캐싱(서비스 워커)은 여전히 범위 밖
- ✅ 2026-08-14 — 카카오 로그인 후 콜백이 `localhost:3000`으로 잘못 리다이렉트되던 버그 수정(`x-forwarded-host` 헤더 사용) — ngrok뿐 아니라 Vercel 배포에서도 필요했던 수정. GitHub(`yechan928/qt-app`)에 레포 생성 + Vercel Hobby 플랜으로 배포 완료(`qt-app-gamma.vercel.app`), Supabase Redirect URL에도 등록. 그룹 목록 카드의 초대 코드 옆에 "복사" 버튼 추가(`CopyInviteCode`)
- ✅ 2026-08-14 — 그룹 화면에 카톡 채팅방 설정 같은 그룹원 목록 사이드 패널 + 그룹 나가기 기능 추가(`GroupMembersPanel`, SPEC.md §4-11). `group_members` RLS를 "내 소속 행만 조회"에서 "내가 속한 그룹의 다른 멤버도 조회 가능"으로 완화(`0007_group_member_visibility.sql`, `is_group_member()` security definer 함수로 자기참조 무한 재귀 회피) + 본인 소속 삭제(그룹 나가기) 정책 추가
- ✅ 2026-08-14 — 아멘 누르고 뒤로가기 하면 반영이 안 된 것처럼 보이는 버그 수정. 처음엔 사파리 bfcache로 오진단해서 `components/BfcacheRefresh.tsx`(pageshow 이벤트 기반)를 레이아웃에 추가했었는데, 사용자가 재확인해보니 여전히 안 됨 → Next.js 16 문서 재확인 결과 **"뒤로가기/앞으로가기 탐색은 staleTimes 설정과 무관하게 항상 Client Cache를 재사용한다"**(레이아웃/스크롤 위치 보존 목적, 공식 문서에 명시)는 게 진짜 원인이었음. `BfcacheRefresh`는 실제 브라우저 문서 언로드가 있을 때만 도움되고, Next 자체 SPA 내비게이션(뒤로가기 포함)에는 효과가 제한적이라 그대로 두되(다른 시나리오 대비 안전망), 진짜 수정은 `components/RefreshOnMount.tsx`(마운트 시 무조건 `router.refresh()`)를 그룹 화면(`groups/[id]`)과 나눔 상세(`posts/[id]`)에 직접 추가하는 것 — 이 두 페이지는 뒤로가기로 재방문될 때마다 컴포넌트가 다시 마운트되므로 매번 최신 데이터를 강제로 다시 가져옴
- ✅ 2026-08-14 — "공유하기" 팝업의 그룹 목록에 같은 그룹이 그룹원 수만큼 중복으로 뜨던 버그 수정. 그룹원 목록 패널 만들면서 `group_members` RLS를 넓힌 부작용 — `GroupShareSelect.tsx`가 `user_id` 필터 없이 `group_members`를 조회해서, 이제는(RLS가 넓어져서) 그룹의 다른 멤버 행까지 다 딸려와 그룹 하나가 멤버 수만큼 중복 표시됐음. `.eq('user_id', 내ID)`를 명시적으로 추가해서 해결(전에는 옛날 RLS가 암묵적으로 이 필터 역할을 해줬던 것이라 코드에 안 써도 됐던 것)
- ✅ 2026-08-25 — 자정~오전 9시(KST) 사이 "오늘의 QT"·나눔 쓰기 등이 어제 날짜로 보이던 버그 수정. `lib/date.ts`의 `todayDateString()`이 실행 환경의 시스템 시간대를 그대로 썼는데, 로컬 개발은 브라우저(한국 시간)라 안 드러났지만 Vercel 서버는 UTC로 도는 게 원인 — UTC+9(KST) 고정 계산으로 수정. 이 함수를 오늘의 QT·나눔 쓰기 캘린더 기본값·말씀 등록 등 여러 곳에서 공용으로 써서 한 번에 다 해결됨
- ✅ 2026-08-25 — 관리자가 "말씀 등록"에서 미래 날짜에 저장하면 화면이 조용히 오늘 날짜로 돌아가서, 모르고 오늘 칸에 또 등록하게 되던 문제 수정. 저장 후 달력을 새로고침(마운트)시키는 과정에서 선택 날짜가 항상 오늘로 초기화되던 게 원인 — `QtDatePicker`가 `initialDate`를 받아서 저장 직후에도 방금 작업하던 그 날짜·달에 그대로 머물도록 수정. 테스트하면서 2026-09-15에 임시 데이터 저장해둠(실제 등록 시 자연스럽게 덮어써짐, 급하면 SQL Editor에서 `delete from qt_schedule where qt_date = '2026-09-15'`로 지워도 됨)
- ✅ 2026-08-25 — "전체 말씀 구절"이 한 장(챕터) 안에서만 절 범위를 지정할 수 있어서, 실제 QT 본문처럼 장을 걸치는 구절(예: 신명기 11:26-12:7)을 아예 등록할 수 없던 문제 수정. `lib/verseRef.ts`의 `parseVerseRef`가 이제 "책 시작장:시작절-끝장:끝절" 형식도 파싱하고(기존 "책 장:절-절" 형식도 그대로 지원), 원문 조회는 `lib/bibleVerses.ts`의 새 `joinVerseRows`로 장 범위째 가져와 절 단위로 정확히 잘라 합침(장이 걸치면 절 앞에 장 번호도 표시). 소주제별 구절 입력도 전체 구절이 한 장이면 기존처럼 절 번호만, 여러 장에 걸치면 "장:절" 형식으로 입력받도록 대응. 테스트하면서 2026-09-20에 임시 데이터(신명기 11:26-12:7) 저장해둠 — 실제 등록 시 자연스럽게 덮어써짐
- ✅ 2026-08-28 — 관리자 "말씀 등록"에서 이미 등록된 날짜인데도 저장/취소가 있는 편집 폼이 그대로 뜨던 버그 수정(로컬 개발 서버에서는 4가지 시나리오로 테스트했을 때 재현이 안 됐는데, 실제 배포 사이트(`qt-app-gamma.vercel.app`)에서는 사용자가 스크린샷으로 재현을 확인함 — **로컬 테스트만으로 "정상 작동"이라 판단한 게 잘못이었음**, 앞으로는 가능하면 배포 사이트에서도 재확인). 원인은 `QTScheduleForm`이 편집 모드 여부를 `useState(existing === null)`로 마운트 시점에 한 번만 정하고 있었던 것 — 저장 직후 `refreshKey`가 바뀌어 이 컴포넌트가 재마운트되는데, 그 순간 `QtDatePicker`의 재조회가 아직 끝나기 전이라 `existing`이 잠깐 낡은 값(null)일 수 있고, 이때 굳어버린 `editing=true`가 이후 최신 데이터가 도착해도 안 바뀜. 로컬은 네트워크가 빨라 이 창이 거의 안 보였지만 배포 환경은 지연이 커서 그대로 굳어 보였던 것으로 추정. `editing` 상태를 `QTScheduleForm` 내부에서 빼서 `ScheduleAdminView`가 소유하도록 옮기고, `QtDatePicker`의 조회가 실제로 끝나는 시점(`handleDateChange`)에만 값을 정하도록 고쳐 타이밍에 의존하지 않게 함(`onStartEdit`/`onCancelEdit`을 props로 내려줌). tsc/eslint/build 통과 확인. **추가 검증(2026-08-28)**: "로컬은 빨라서 통과하는 거 아니냐"는 지적을 받아, Playwright + Chrome DevTools Protocol `Network.emulateNetworkConditions`로 로컬에서도 지연 600ms/대역폭 제한을 인위적으로 걸어 재현 — 수정 전 코드는 이 조건에서 신규 등록 저장 직후 빈 등록 폼에 4초 넘게(관찰 종료 시점까지) 그대로 멈춰 있는 걸 확인했고(사용자가 보여준 스크린샷과 동일한 증상), 같은 조건에서 수정 후 코드는 짧은 전환 렌더(약 650ms 지점에 한 틱) 이후 항상 올바른 읽기 전용 화면으로 안정적으로 정착함을 확인함 — 네트워크 속도에 우연히 기댄 게 아니라 구조적으로 고쳐졌음을 확인. 다만 이 방법도 로컬 서버(`localhost:3000`) 기준이며, Claude는 카카오 로그인을 대신 완료할 수 없어 배포 사이트(`qt-app-gamma.vercel.app`)에서 직접 재현 테스트는 여전히 못 함 — 실사용 중 다시 보이면 알려줄 것. 이 검증 과정에서 2026-11-05, 2026-12-12, 2027-01-20에 임시 데이터 저장해둠(실제 등록 시 자연스럽게 덮어써짐)
- ✅ 2026-09-02 — 소주제별 "적용 질문" 기능 추가. QT 교재처럼 소주제 구절마다 적용 질문이 딸려있는데, 매번 다 펼쳐두면 산만해 보여서 구절 밑에 작은 "?" 버튼으로 숨겨뒀다가 눌러야 펼쳐지게 함. `QtSection` 타입에 선택 필드 `question` 추가(마이그레이션 불필요 — `sections`가 이미 JSON 컬럼이라 새 키가 그냥 저장됨), 관리자 입력 폼(`QtSectionEditor`)에 소주제별 텍스트 영역 추가, 표시 쪽은 새 공용 컴포넌트 `QtSectionCard`로 토글 동작을 한 곳에 모아서 오늘의 QT(`QTDayView`)·나눔 쓰기(`PostForm`)·나눔 미리보기(`NanumPreview`) 세 화면에 동일하게 적용(관리자 자신의 등록 읽기 전용 화면(`QTScheduleForm`)에는 굳이 숨길 필요 없어서 토글 없이 그대로 노출, 범위 밖으로 뺌). 저작권 노트(§4-7)에 적용 질문도 소제목·요약과 같은 원칙(관리자 수동 입력 허용) 적용된다고 명시. Playwright로 미래 날짜에 등록 후 오늘의 QT·나눔 쓰기 화면 양쪽에서 "클릭 전엔 안 보이다가 클릭하면 보임"을 확인 — **이 테스트 중 캘린더 기본 선택 날짜가 "오늘"이라는 걸 깜빡하고 그날 실제 등록된 내용을 테스트용으로 덮어쓴 실수가 있었음(사용자가 다시 등록해서 복구)**, 앞으로 로컬 테스트는 절대 오늘 날짜를 건드리지 않고 항상 먼 미래 날짜로만 할 것
